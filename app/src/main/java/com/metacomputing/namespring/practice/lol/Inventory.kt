package com.metacomputing.namespring.practice.lol

class Inventory(
    var items: ArrayList<Item> = ArrayList()
) {
    fun add(item: Item) {
        // item.apply(champion) ???????????????????
    }
}